const TwoColumn = ({data}) => {
    const {
        variant,
        heading,
        subheading,
        description,
        image,
        flexDirection,
        price
    } = data;
    return (
        <>
            two col
        </> 
    );
}
 
export default TwoColumn;